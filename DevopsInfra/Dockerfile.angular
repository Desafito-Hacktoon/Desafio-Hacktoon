# Etapa de build
FROM node:20.19-alpine
WORKDIR /app
COPY . .
RUN npm install && npm run build --prod

# Etapa final: Nginx servindo Angular
FROM nginx:alpine
COPY --from=builder /app/dist/heatMap-app /usr/share/nginx/htmlgit